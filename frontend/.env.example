# Base URL of the backend API.
# - Local dev: leave unset, Vite's proxy in vite.config.js forwards /api to localhost:3000
# - Production (Netlify): set this to your Railway backend URL, e.g.
#   VITE_API_URL=https://web-production-c0c76.up.railway.app/api
VITE_API_URL=
